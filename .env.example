# PostgreSQL bağlantısı
DATABASE_URL="postgresql://marketplace:marketplace@127.0.0.1:5432/marketplace?schema=public"

# İmzalı endirmə linklərini imzalayan açar.
# İSTEHSALDA MÜTLƏQ DƏYİŞİN — ən azı 32 simvol təsadüfi dəyər:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DOWNLOAD_SECRET="dev-only-change-me-in-production-please-32bytes"

# Satılan faylların saxlandığı kök qovluq (public/-dən kənarda olmalıdır)
STORAGE_ROOT="./storage"
